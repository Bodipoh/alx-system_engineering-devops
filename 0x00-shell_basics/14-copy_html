#!/bin/bash
find . -maxdepth 1 -type f -name "*.html" | while read -r file; do base_file=$(basename "$file") destination="$parent_dir/$base_file" if [[ ! -e "$destination" || "$file" -nt "$destination" ]]; then cp "$file" "$destination" echo "Copied $base_file to $parent_dir" fi done
